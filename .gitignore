# gitignore 파일에 대해 더 자세히 알고 싶으시면 다음 링크를 참조하세요: https://help.github.com/articles/ignoring-files/

# git에 의해 추적되지 않도록 하여 github에 업로드되지 않도록 하는 항목들입니다.

# dependencies (의존성): node_modules, pnp 관련 파일들은 git에 의해 추적되지 않으므로 github에 업로드되지 않습니다.
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing (테스트): coverage 폴더는 git에 의해 추적되지 않으므로 github에 업로드되지 않습니다.
/coverage

# next.js: next.js 관련 폴더들은 git에 의해 추적되지 않으므로 github에 업로드되지 않습니다.
/.next/
/out/

# production (production 빌드): 빌드 폴더는 git에 의해 추적되지 않으므로 github에 업로드되지 않습니다.
/build

# misc (기타): .DS_Store, *.pem 파일들은 git에 의해 추적되지 않으므로 github에 업로드되지 않습니다.
.DS_Store
*.pem

# debug (디버그): 디버그 로그 파일들은 git에 의해 추적되지 않으므로 github에 업로드되지 않습니다.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (로컬 환경 파일): 로컬 환경 파일들은 git에 의해 추적되지 않으므로 github에 업로드되지 않습니다.
.env*.local

# vercel: vercel 관련 파일들은 git에 의해 추적되지 않으므로 github에 업로드되지 않습니다.
.vercel

# typescript (타입스크립트): 타입스크립트 관련 파일들은 git에 의해 추적되지 않으므로 github에 업로드되지 않습니다.
*.tsbuildinfo
next-env.d.ts
